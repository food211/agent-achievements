interface:
  display_name: "五行 Harness"
  short_description: "迭代旧规则，把 AI 协作痛点沉淀成新规则"
  default_prompt: "使用 $wuxing-harness 诊断当前仓库。先让我选择：迭代已有规则，或从 AI 协作痛点沉淀新规则；选定后建立任务追踪并开始对应流程。当前入口完成后，再询问是否进入另一个。"
policy:
  allow_implicit_invocation: true
