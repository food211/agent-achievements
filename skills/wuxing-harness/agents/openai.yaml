interface:
  display_name: "五行 Harness"
  short_description: "带着你逐条修改或退役已经失效的 Agent 规则"
  default_prompt: "使用 $wuxing-harness 诊断当前仓库。先扫描实际生效的规则并给出证据，再一次只带我处理一条陈旧规则；不要把问题丢给我从零回答。"
policy:
  allow_implicit_invocation: true
