interface:
  display_name: "五行 Harness"
  short_description: "审查 Agent 规则漂移，带证据提出修改"
  default_prompt: "Use $wuxing-harness to audit my workspace rules against current code, tests, and run evidence."
policy:
  allow_implicit_invocation: true
