interface:
  display_name: "五行 Harness"
  short_description: "审查规则漂移，把改进接入 Agent 成就闭环"
  default_prompt: "Use $wuxing-harness to audit my workspace rules, apply only approved changes, and report verified improvements to Agent Achievements."
policy:
  allow_implicit_invocation: true
